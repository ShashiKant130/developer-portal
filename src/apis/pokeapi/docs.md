# PokéAPI — Getting Started

Use the sandbox to call live endpoints against `https://pokeapi.co/api/v2`.

## Quick example

Try `GET /pokemon/pikachu` to fetch a Pokémon resource by name.

## Authentication

PokéAPI is public; no API key is required for sandbox requests.
